version: '3.8'

services:
  dropos-engine:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount the database to persist data across container restarts
      - ./backend/dropship.db:/app/backend/dropship.db
    restart: unless-stopped
